development:
  adapter: async

test:
  adapter: async

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://rediscloud:password@localhost:6379" } %>
  channel_prefix: tech2try-rails_production
